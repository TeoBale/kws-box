# KWS Box

Configure an Ubuntu/Debian machine for SSH work by installing Tmux, uv,
Bun, Docker, Agy CLI, Codex, OpenCode and Pi. The script can be executed more
Times: existing tools are not reinstalled.

```bash
curl -fsSL https://raw.githubusercontent.com/TeoBale/kws-box/main/setup.sh | bash
```
